import { PulseLoader } from "react-spinners";

function Spinner() {
  return (
    <div id="spinner">
      <PulseLoader color="#3662e3" cssOverride={{}} size={8} />
    </div>
  );
}

export default Spinner;
